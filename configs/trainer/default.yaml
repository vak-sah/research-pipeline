_target_: lightning.pytorch.Trainer
max_epochs: 10
accelerator: auto      # precision is set per-device at runtime by the device plan
devices: auto
precision: 32-true
log_every_n_steps: 20
num_sanity_val_steps: 0
enable_progress_bar: true
